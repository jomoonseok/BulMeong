<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.gdu.bulmeong.review.mapper.ReviewBoardMapper">

	<!-- 1. 게시글 카운트 -->
	<select id="reviewListCount" resultType="int">
		SELECT COUNT(*)
		  FROM REVIEW_BRD
	</select>

	<!-- 2. 게시글 리스트 -->
	<select id="reviewList" parameterType="Map" resultType="ReviewBoardDTO">
		SELECT A.REVIEW_NO, A.NICKNAME, A.REVIEW_TITLE, A.REVIEW_CREATE_DATE, A.REVIEW_HIT
		 FROM REVIEW_BRD A
		 ORDER BY A.REVIEW_NO DESC
		 LIMIT #{begin}, #{recordPerPage}
    </select>

	<!-- 3. 조회수 -->
	<update id="updateHit" parameterType="int">
		UPDATE REVIEW_BRD
		   SET REVIEW_HIT = REVIEW_HIT + 1
		 WHERE REVIEW_NO = #{freeNo}
	</update>
	
	
</mapper>